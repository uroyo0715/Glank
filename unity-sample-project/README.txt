これは「GlankSampleGame」プロジェクトからダウンロードできる、Unityのサンプルプロジェクトの
置き場所です（プレースホルダー）。

実際のUnityプロジェクト一式をこのディレクトリに配置すると、
GET /api/v1/sample-project/download がそのままzip化して配布するようになります
（server/src/routes/sdk.js参照）。差し替え以外のコード変更は不要です。
